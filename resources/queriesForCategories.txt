PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
PREFIX mpr: <http://www.semanticweb.org/ser531/ontologies/MissingPersonReport#>
SELECT *
WHERE {
  ?victim a mpr:Victim;
          rdfs:Legal_Name ?name;
          rdfs:hasCityOfOrigin ?city_of_origin;
          rdfs:hasCountyOfOrigin ?county_of_origin;
          rdfs:Missing_Age ?missing_age;
    	  rdfs:isOfAge ?age;
       	  rdfs:Case_Number ?case_number;
    	  rdfs:Bio_Sex ?sex;
    	  mpr:hasCircumstanceOfDisappearance ?cod;
    	  rdfs:Race_Ethnicity ?race;
     	  rdfs:hasMapData ?map;
    	  rdfs:Coordinates ?cords;
    	  rdfs:DLC ?DLC;
    	  mpr:columnURI ?namUS;
    	  mpr:hasImageURL ?image;
     	  FILTER (
            CONTAINS(LCASE(STR(?cod)), LCASE("abducted by")) &&
            CONTAINS(LCASE(STR(?cod)), LCASE("mother"))
          )
}

PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
PREFIX mpr: <http://www.semanticweb.org/ser531/ontologies/MissingPersonReport#>
SELECT *
WHERE {
  ?victim a mpr:Victim;
          rdfs:Legal_Name ?name;
          rdfs:hasCityOfOrigin ?city_of_origin;
          rdfs:hasCountyOfOrigin ?county_of_origin;
          rdfs:Missing_Age ?missing_age;
    	  rdfs:isOfAge ?age;
       	  rdfs:Case_Number ?case_number;
    	  rdfs:Bio_Sex ?sex;
    	  mpr:hasCircumstanceOfDisappearance ?cod;
    	  rdfs:Race_Ethnicity ?race;
     	  rdfs:hasMapData ?map;
    	  rdfs:Coordinates ?cords;
    	  rdfs:DLC ?DLC;
    	  mpr:columnURI ?namUS;
    	  mpr:hasImageURL ?image;
     	  FILTER (
            CONTAINS(LCASE(STR(?cod)), LCASE("abducted by")) &&
            CONTAINS(LCASE(STR(?cod)), LCASE("father"))
          )
}

PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
PREFIX mpr: <http://www.semanticweb.org/ser531/ontologies/MissingPersonReport#>
SELECT *
WHERE {
  ?victim a mpr:Victim;
          rdfs:Legal_Name ?name;
          rdfs:hasCityOfOrigin ?city_of_origin;
          rdfs:hasCountyOfOrigin ?county_of_origin;
          rdfs:Missing_Age ?missing_age;
    	  rdfs:isOfAge ?age;
       	  rdfs:Case_Number ?case_number;
    	  rdfs:Bio_Sex ?sex;
    	  mpr:hasCircumstanceOfDisappearance ?cod;
    	  rdfs:Race_Ethnicity ?race;
     	  rdfs:hasMapData ?map;
    	  rdfs:Coordinates ?cords;
    	  rdfs:DLC ?DLC;
    	  mpr:columnURI ?namUS;
    	  mpr:hasImageURL ?image;
     	  FILTER (
            CONTAINS(LCASE(STR(?cod)), LCASE("foul play")) &&
            CONTAINS(LCASE(STR(?cod)), LCASE("suspected"))
          )
}

PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
PREFIX mpr: <http://www.semanticweb.org/ser531/ontologies/MissingPersonReport#>
SELECT *
WHERE {
  ?victim a mpr:Victim;
          rdfs:Legal_Name ?name;
          rdfs:hasCityOfOrigin ?city_of_origin;
          rdfs:hasCountyOfOrigin ?county_of_origin;
          rdfs:Missing_Age ?missing_age;
    	  rdfs:isOfAge ?age;
       	  rdfs:Case_Number ?case_number;
    	  rdfs:Bio_Sex ?sex;
    	  mpr:hasCircumstanceOfDisappearance ?cod;
    	  rdfs:Race_Ethnicity ?race;
     	  rdfs:hasMapData ?map;
    	  rdfs:Coordinates ?cords;
    	  rdfs:DLC ?DLC;
    	  mpr:columnURI ?namUS;
    	  mpr:hasImageURL ?image;
     	  FILTER (
            CONTAINS(LCASE(STR(?cod)), LCASE("disappeared")) &&
            CONTAINS(LCASE(STR(?cod)), LCASE("suspicious")) &&
            CONTAINS(LCASE(STR(?cod)), LCASE("circumstances"))

          )
}
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
PREFIX mpr: <http://www.semanticweb.org/ser531/ontologies/MissingPersonReport#>
SELECT *
WHERE {
  ?victim a mpr:Victim;
          rdfs:Legal_Name ?name;
          rdfs:hasCityOfOrigin ?city_of_origin;
          rdfs:hasCountyOfOrigin ?county_of_origin;
          rdfs:Missing_Age ?missing_age;
    	  rdfs:isOfAge ?age;
       	  rdfs:Case_Number ?case_number;
    	  rdfs:Bio_Sex ?sex;
    	  mpr:hasCircumstanceOfDisappearance ?cod;
    	  rdfs:Race_Ethnicity ?race;
     	  rdfs:hasMapData ?map;
    	  rdfs:Coordinates ?cords;
    	  rdfs:DLC ?DLC;
    	  mpr:columnURI ?namUS;
    	  mpr:hasImageURL ?image;
     	  FILTER (
            CONTAINS(LCASE(STR(?cod)), LCASE("flight")) &&
            CONTAINS(LCASE(STR(?cod)), LCASE("avoid")) &&
            CONTAINS(LCASE(STR(?cod)), LCASE("prosecution"))

          )
}
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
PREFIX mpr: <http://www.semanticweb.org/ser531/ontologies/MissingPersonReport#>
SELECT *
WHERE {
  ?victim a mpr:Victim;
          rdfs:Legal_Name ?name;
          rdfs:hasCityOfOrigin ?city_of_origin;
          rdfs:hasCountyOfOrigin ?county_of_origin;
          rdfs:Missing_Age ?missing_age;
    	  rdfs:isOfAge ?age;
       	  rdfs:Case_Number ?case_number;
    	  rdfs:Bio_Sex ?sex;
    	  mpr:hasCircumstanceOfDisappearance ?cod;
    	  rdfs:Race_Ethnicity ?race;
     	  rdfs:hasMapData ?map;
    	  rdfs:Coordinates ?cords;
    	  rdfs:DLC ?DLC;
    	  mpr:columnURI ?namUS;
    	  mpr:hasImageURL ?image;
     	  FILTER (
            CONTAINS(LCASE(STR(?cod)), LCASE("assisted")) &&
            CONTAINS(LCASE(STR(?cod)), LCASE("living"))

          )
}